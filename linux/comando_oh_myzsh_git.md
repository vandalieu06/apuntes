# OH MY ZSH

```bash
sudo apt install zsh
zsh --version

chsh -s /usr/bin/zsh
echo $SHELL

sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohm...)"

sudo apt-get install fonts-powerline

git clone --depth=1 https://github.com/romkatv/powerlevel... ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k
ZSH_THEME="powerlevel10k/powerlevel10k"

sudo apt-get install dconf-cli
git clone https://github.com/dracula/gnome-term...
cd gnome-terminal
./install.sh

git clone https://github.com/zsh-users/zsh-auto... $ZSH_CUSTOM/plugins/zsh-autosuggestions 
git clone https://github.com/zsh-users/zsh-synt... $ZSH_CUSTOM/plugins/zsh-syntax-highlightingsudo apt install zsh
zsh --version

chsh -s /usr/bin/zsh
echo $SHELL

sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohm...)"

sudo apt-get install fonts-powerline

git clone --depth=1 https://github.com/romkatv/powerlevel... ${ZSH_CUSTOM:-$HOME/.oh-my-zsh/custom}/themes/powerlevel10k
ZSH_THEME="powerlevel10k/powerlevel10k"

sudo apt-get install dconf-cli
git clone https://github.com/dracula/gnome-term...
cd gnome-terminal
./install.sh

git clone https://github.com/zsh-users/zsh-auto... $ZSH_CUSTOM/plugins/zsh-autosuggestions 
git clone https://github.com/zsh-users/zsh-synt... $ZSH_CUSTOM/plugins/zsh-syntax-highlighting
```
